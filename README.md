# Tessa_app

to run the project you need to go in the main folder and run **npx expo start --tunnel** with expo go application.

for run : npx expo start --tunnel

for build : eas build -> aab

for preview : eas build -p android --profile preview ->apk

for update preview version : eas update --branch production --message "update"
